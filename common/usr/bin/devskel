#!/bin/bash

SKELDIR=/etc/devskel
DESTDIR="$HOME"

set -e

cp -vu "$SKELDIR/zshrc" "$DESTDIR/.zshrc"
git clone https://github.com/UncleSniper/vim-scripts.git "$DESTDIR/vim-scripts"
"$DESTDIR/vim-scripts/install.sh"
